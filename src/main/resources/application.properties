# Server Configuration
server.port=8080
spring.application.name=weather-aqi-backend

# Weather API Configuration
weather.api.key=70eb6858779947baa7963224251911
weather.api.base-url=http://api.weatherapi.com/v1

# CORS Configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

# Cache Configuration
spring.cache.type=simple
cache.ttl.weather=900
cache.ttl.search=3600

# Logging
logging.level.com.weatheraqi=DEBUG
logging.level.org.springframework.web=INFO
